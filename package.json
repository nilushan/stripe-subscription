{
  "name": "stripe-subscription",
  "version": "0.1.0",
  "description": "Stripe subscription integration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "start": "run-p --race start:frontend start:backend",
    "build:shared": "tsc --project shared",
    "build:frontend": "cd frontend && npm install",
    "build:backend": "cd backend && npm install",
    "build": "run-p build:shared build:frontend build:backend"
  },
  "author": "nilushan-bluekey",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.44.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.1.6"
  }
}
